# frozen_string_literal: true

source('https://rubygems.org')

gemspec

group(:test) do
  gem('potluck-nginx', path: File.join(__dir__, 'potluck-nginx'))
  gem('potluck-postgres', path: File.join(__dir__, 'potluck-postgres'))
end
